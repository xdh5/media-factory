# 心灵鸡汤可视化时间轴工具

`generate_quiz_timeline_ass` 生成一份独立 ASS 图层：选项在朗读完成后按 A、A+B、A+B+C、A+B+C+D 累积显示，结果按 A、B、C、D 逐个点亮。整条视频同时按内容划分为多个章节，每段显示一个简短标题，章节宽度按真实时长等比例计算，当前章节自动高亮。

章节条可通过 `position` 配置为 `bottom` 或 `top`。图层负责标示内容分段和时间位置，用户实际拖动视频仍使用平台播放器原生进度条。
