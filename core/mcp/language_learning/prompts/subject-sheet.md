为“{topic}”制作一张语言学习卡片的共用主体素材图。

输出规格：
- 只输出 1 张 PNG，横版 16:9 纯色；优先 1920×1080，其他 16:9 尺寸也可。
- 画布分为 2 行×5 列，共 10 格；每格只放 1 个完整主体。
- 每个主体完整、独立，并与其他主体明显分离；允许靠近格子边缘，但主体不得越过格子边界或被裁切。
- 主体之间不得接触或重叠。
- 2×5 仅表示主体的位置顺序，禁止画出网格线、边框或分隔线。
- 整张图片绝对禁止出现任何文字、字母、数字、单词、标题、标签、注释、标牌、水印、品牌和排版元素。
- 下方“主体顺序”中的英文仅用于告诉你每格画什么，绝对不能把这些英文画进图片。
- 每格只能画对应物体本身；如果物体带显示屏、包装或铭牌，也必须保持空白，不得出现字符。

主体顺序（从左到右、从上到下）：
{word_list}
