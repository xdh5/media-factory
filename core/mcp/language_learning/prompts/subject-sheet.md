为“{topic}”制作一张语言学习卡片的共用主体素材图。

输出规格：
- 只输出 1 张 PNG，横版 16:9；1920×1080。
- 请自行选择一种与十个主体颜色都形成强烈反差、且主体内部极少使用的高饱和背景色。
- 整张画布必须使用完全均匀、无纹理、无渐变的同一种纯色背景；除十个主体外不得出现第二种背景颜色。
- 画布分为 2 行×5 列，共 10 格；每格只放 1 个完整主体。
- 每个主体完整、独立，并与其他主体明显分离，但主体不得越过格子边界或被裁切。
- 主体之间不得接触或重叠。
- 每个主体的最外层轮廓必须单一、连续、清晰且边缘平滑。
- 主体外轮廓禁止使用彩色描边，禁止出现双重或多重描边、套边、杂色边缘及颜色溢出。
- 主体边缘禁止出现锯齿、毛边、断裂、模糊或像素化，细小结构也必须保持轮廓干净利落。
- 所有主体禁止出现投影、落地阴影、光晕、倒影或背景色描边。
- 必须严格只生成下面列出的十个主体，不得重复任何主体，也不得增加装饰物、容器、餐具或其他附属物。
- 2×5 仅表示主体的位置顺序，禁止画出网格线、边框或分隔线。
- 整张图片绝对禁止出现任何文字、字母、数字、单词、标题、标签、注释、标牌、水印、品牌和排版元素。

主体顺序（从左到右、从上到下）：
{word_list}
