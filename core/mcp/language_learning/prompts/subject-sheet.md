为“{topic}”制作一张语言学习卡片的共用主体素材图。

输出规格：
- 只输出 1 张 PNG，横版 16:9；1920×1080。
- 必须优先直接输出带真实 Alpha 通道的透明背景 PNG，主体之外的像素必须透明；不得用黑色、白色或棋盘格假装透明。
- 只有当前生图能力确实无法输出透明背景 PNG 时，才允许使用与十个主体颜色都形成强烈反差、且主体内部极少使用的单一纯色背景，供后续自动抠图兜底。
- 使用纯色兜底时，整张画布必须使用完全均匀、无纹理、无渐变的同一种纯色背景；除十个主体外不得出现第二种背景颜色。
- 画布分为 2 行×5 列，共 10 格；每格只表达 1 个单词对应的完整视觉主体或紧凑小场景。
- 每个主体完整、独立，并与其他主体明显分离，但主体不得越过格子边界或被裁切。
- 主体之间不得接触或重叠。
- 每个主体的最外层轮廓必须单一、连续、清晰且边缘平滑。
- 主体外轮廓禁止使用彩色描边，禁止出现双重或多重描边、套边、杂色边缘及颜色溢出。
- 主体边缘禁止出现锯齿、毛边、断裂、模糊或像素化，细小结构也必须保持轮廓干净利落。
- 主体外部不得出现投影、光晕、倒影、白边、黑边或背景色描边；允许并鼓励主体内部使用自然明暗、柔和高光和材质细节来表现体积，禁止把主体画成没有层次的纯色块。
- 主体必须采用精致、自然、可信的全彩插画表现：使用符合现实物体的多种固有色和材质色，不得把全部主体统一染成同一种颜色，也不得使用单色线稿、白色剪影、描边图标、填色书风格、廉价素材图标或塑料玩具质感。
- 每个主体应有清楚的结构、比例、材质和必要细节，远看易识别，近看不空洞；不得用三角形、圆形、箭头、循环符号等抽象 UI 图标代替真实物体或含义。
- 对地点、职业、服务、动作或抽象概念，可在单格内使用一个构图紧凑、关系明确的小场景表达含义，并包含理解该词必需的人物或物件；这些元素共同视为一个视觉主体。不得增加与词义无关的装饰物。
- 必须严格只生成下面列出的十个语义主体，不得重复，也不得添加第十一个无关主体。
- 2×5 仅表示主体的位置顺序，禁止画出网格线、边框或分隔线。
- 严禁把主体清单中的英语原词、对应中文或韩文翻译作为标签、标题或画面文字直接写进图片，也不得在主体下方生成标签。
- 与目标词无关、且为场景表达所必需的自然文字可以保留，例如其他店铺招牌、路标、包装或屏幕上的正常文字；不得借此加入水印、品牌标识或平台角标。
- 对登机牌、证件、票据、标牌、柜台、屏幕、包装等通常带文字或条码的物体，若文字本身就是要教学的目标词或其翻译，必须改为无字外形；其他自然文字可保留，但不能喧宾夺主。

主体顺序（从左到右、从上到下）：
{word_list}
