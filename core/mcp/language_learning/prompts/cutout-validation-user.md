检查这一张透明主体抠图。透明区域在预览中可能显示为白色画布，请沿主体全部外轮廓放大观察。

必须同时满足：

1. 只有一个主体，且主体完整；把手、尖端、边缘、孔洞等均未被裁断。
2. 没有混入相邻主体或无关物体。
3. 主体轮廓四周没有残留原主题图的纯色背景；不得出现彩色细边、背景色描边、光晕、色块、毛边或锯齿状颜色污染。
4. 主体内部真实存在的颜色、反光和纹理不能误判为背景残留；只把紧贴外轮廓或透明区域的异常背景色判为色边。

只返回以下 JSON，不要 Markdown 或解释：

{
  "valid": true,
  "failure_kind": "",
  "issue": ""
}

失败类型：

- 裁剪框切断主体或混入相邻物体：`failure_kind` 为 `crop`。
- 原图主体本身残缺、重叠或被背景色吃掉：`failure_kind` 为 `source`。
- 抠图轮廓残留原背景色的彩边、描边、光晕、色块或毛边：`failure_kind` 为 `background_edge`；`issue` 必须说明残留颜色和大致位置，以便下一次改用不同背景色重新生成。
