检查这一张透明主体抠图。透明区域在预览中可能显示为白色画布，请沿主体全部外轮廓放大观察。

唯一判定标准：主体轮廓四周或透明区域附近是否残留原主题图的纯色背景，包括彩色细边、背景色描边、光晕、色块、毛边或锯齿状颜色污染。

不要检查主体是否完整、是否只有一个主体、是否混入相邻物体，也不要检查文字、水印、画风或内容是否正确。主体内部真实存在的颜色、反光和纹理不能误判为背景残留。

只返回以下 JSON，不要 Markdown 或解释：

{
  "valid": true,
  "failure_kind": "",
  "issue": ""
}

只有发现背景残色时才判定失败，`failure_kind` 固定为 `background_edge`；`issue` 必须说明残留颜色和大致位置，以便下一次改用不同背景色重新生成。
