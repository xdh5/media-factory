检查这张去背景后的完整主题图。画布为 2 行×5 列，共 10 个主体，按上排从左到右、下排从左到右排序。

必须逐项检查：
1. 数量：是否正好 10 个彼此独立的主体
2. 完整性：每个主体是否完整、未被裁切、未与其他主体重叠或粘连
3. 文字：是否出现任何文字、字母、数字、单词、标题、标签、注释或标牌
4. 水印：是否出现水印、品牌标识或平台角标
5. 画风与质感：是否统一为精致、自然、可信的全彩插画；主体是否具有符合现实的多种固有色、材质细节和内部明暗，且不存在全体单色染色、大片无层次纯色块、白色剪影、单色线稿、描边图标、填色书风格、廉价素材感、塑料玩具感或明显风格混杂
6. 语义表达：地点、职业、服务、动作或抽象概念是否由可理解的真实物体或紧凑小场景表达；若使用三角形、圆形、箭头、循环符号等抽象 UI 图标代替词义，必须判为失败
7. 透明边缘：主体轮廓或透明区域附近是否存在非常严重、影响观感的白边、黑边、彩边、描边、光晕、色块或毛边

只返回以下 JSON：

{
  "valid": true,
  "failure_kind": "",
  "issue": "",
  "subject_count": 10
}

只有发现问题时才判定失败。`failure_kind` 必须从以下取值中选择一个：
- `background_edge`：透明边缘或抠图残色
- `text`：出现文字
- `watermark`：出现水印
- `style`：画风、配色、质感或语义表达不合格
- `count`：主体数量不是 10 个
- `completeness`：主体不完整、重叠或粘连

`issue` 必须具体说明问题和大致位置，便于下一次重新生成修正。
