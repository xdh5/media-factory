只定位画面中上排从左到右、下排从左到右的 10 个主体。不要检查或报告文字、水印、画风、内容正确性、主体完整性或背景颜色。

阴影、把手、锅柄、孔洞、反光和物体内部结构都属于对应主体，不得拆成额外边界框。

只返回以下 JSON，不要 Markdown 或解释：

{
  "boxes": [
    [左, 上, 右, 下]
  ],
  "reason": "一句中文定位说明"
}

boxes 必须正好包含 10 个主体的边界框，按上排从左到右 5 个、再按下排从左到右 5 个排序。坐标使用 0 到 1000 的整数相对坐标。

边界框必须保守：完整包含对应主体及其把手、尖端、边缘、孔洞和阴影，框的四周要额外保留明显的纯色背景安全边距。宁可多包含背景，也绝不能让边界框贴住或裁掉主体；不要包含相邻主体。
